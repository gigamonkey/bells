{
  "type": "module",
  "scripts": {
    "dev": "trap 'kill 0' EXIT; npx serve . -l 8081 & ./node_modules/.bin/esbuild bells.js --watch --sourcemap --bundle --format=esm --outfile=out.js"
  },
  "dependencies": {
    "@js-temporal/polyfill": "^0.5.1",
    "@peterseibel/bells": "^0.3.0",
    "@peterseibel/bhs-calendars": "^2.3.1",
    "cors": "^2.8.6",
    "esbuild": "0.28.0",
    "temporal-polyfill": "^0.3.2"
  }
}
